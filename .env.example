# PostgreSQL connection string (Neon or local). Required by the backend.
# Copy this file to `.env` and fill in your credentials.
# The backend reads this via load_dotenv(); it is gitignored and never committed.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
